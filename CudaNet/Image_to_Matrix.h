#pragma once
#include <vector>
using namespace std;
void matrix_fun(vector<vector<int>> &Matrix, int final_number);
void namefunc(char lastname[], int number);
void Console_out(vector<vector<int>> &Matrix, int number_of_image);
void Cimg_out(vector<vector<int>> &Matrix, int number_of_image);
void Console_out_of_range(vector<vector<int>> &Matrix, int iterator);
void Cimg_out_of_range(vector<vector<int>> &Matrix, int iterator);